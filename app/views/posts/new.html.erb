<h1>Write your post <%= current_user.name if current_user%> </h1>

<%= form_with model:@post do |form| %>
<%= form.label :body, "write some gossip", class: 'label' %>
 <%= form.text_area :body %>
 <%= form.submit 'Create post' %>
<% end %>